const myArr = [0, 1, 2, 3, 4, 5]
const myHeroes = ["shaktiman", "nagraj"]
const myArr2 = new Array(1,2,3,4)
// console.log(myHeroes);
// myArr.push(6)                                 // 6 will be added at last
// myArr.pop()                                   // it will delete last element 
// myArr.unshift(9)                              // it will add 9 at starting
//myArr.shift()                                  // it will delete element from starting
// console.log(myArr.includes(9));               // it will check whether the 9 is present in the array or not 
// console.log(myArr.indexOf(3));                   // it will return the index of the element 
// const newArr = myArr.join()                      // adds all the element of an array into a string
// console.log(myArr);                              // here it will returns an array [ 0, 1, 2, 3, 4, 5 ]
// console.log(newArr);                             // and the new array will be returned as a string (0,1,2,3,4,5)

